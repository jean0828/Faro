# =============================================================================
# Faro — Python Dependencies
# =============================================================================

# API Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.10
pydantic-settings>=2.3

# Deep web scraping & browser automation
crawl4ai==0.8.6

# Async HTTP client (used in intel.py for breach API calls)
httpx>=0.27.2
aiohttp>=3.11.11  # crawl4ai>=0.8.6 requires >=3.11.11

# Data processing & parsing
beautifulsoup4==4.12.3
tldextract==5.1.2

# Security header analysis
cryptography>=45.0.7  # pyOpenSSL>=25.3.0 (via crawl4ai) requires >=45.0.7

# Structured output / reporting
rich>=13.9.4           # crawl4ai>=0.8.6 requires >=13.9.4
jinja2==3.1.4         # HTML report templates (future)

# Dashboard (Streamlit frontend)
streamlit>=1.35.0
pandas>=2.2.0

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0       # Retry logic for external calls
